package bonusAssignmentsCMSC201;

public class QuizThreeTests {

	public static void main(String[] args) {
		
		
		/*
			Attempt 1: Question 1
		int mask = 0;
		int count = 0;
		if ((( 5 < 7) || (++count < 10)) | mask++ < 10) mask = mask = + 1;
		if (( 6 > 8) ^ false)      mask = mask = + 10;
		if (!(mask > 1) && ++count > 1)     mask = mask = + 100;
		System.out.print(mask + " " + count);
		// It displays 1 1
		// mask = 1, count = 1
		 */
		
		
		/*
			Attempt 1: Question 5
		int x = 0;
		if (x > 0);
		{
			System.out.println("x");
		}
		// The symbol x is always printed
		 */
		
		
		/*
			Attempt 2: Question 9
		System.out.println((5 + (!2 + 8) * 3 - 3 % 2 ) / 2 );
		// No result, because of !2. A compile eror.
		 */ 
	
		
	}
}